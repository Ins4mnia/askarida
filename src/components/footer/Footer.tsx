import { NextPage } from "next";
import s from "./Footer.module.scss";

interface Props {}

const Footer: NextPage<Props> = ({}) => {
  return <div className={s.Footer}>1</div>;
};

export default Footer;
